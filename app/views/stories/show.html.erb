<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @story.name %>
  <% @story.words.each do |wordobj| %>
  <li>Word: <%= wordobj.name %>
  <ul>Sentence: <%= wordobj.sentence %></ul>
  <ul>Definition: <%= wordobj.definition %></ul>
  </li>
  <% end %>
</p>


<%= link_to 'Edit', edit_story_path(@story) %> |
<%= link_to 'Back', stories_path %>
