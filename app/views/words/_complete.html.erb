<%= form_for word, :remote => true do |f| %>
  <%= f.check_box :is_complete %>
  <%= f.submit "Update" %>
  <%= link_to "(remove)", word, :method => :delete, :data => {:confirm => "Are you sure?"}, :remote => true %>
<% end %>